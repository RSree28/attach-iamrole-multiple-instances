# attach-iamrole-multiple-instances
Attaching IAM role to multiple instances at one shot 

If we are running in Lambda console,attach the following to the Lambda execution role
Amaon Ec2 full access
IAM full access



